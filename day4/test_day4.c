#include <stdio.h>

/*int main(void)
{
	char arr1[] = "Hello World!";

	printf("%s\n", arr1);
	printf("%d\n", sizeof(arr1)); // 글자 12개 + \0(null) = 글자 13개
	printf("%d\n", sizeof(arr1)); // 글자 12개 + \0(null) = 글자 13개
	
	for (int i = 0; i < sizeof(arr1); i++)
	{
		
		printf("%c", arr1[i]);

		if (i == (sizeof(arr1) - 2))
			break;

		printf(", ");
	}
	


	int int_arr[3] = { 10, 20, 30 };
	double double_arr[3] = {1.0, 2.0, 3.0};

	printf("%d, %g\n", int_arr[0], double_arr[0]); // 10, 1
	printf("%d, %g\n", *int_arr, *double_arr);     // 10, 1

	*int_arr += 10;
	*double_arr += 10.5;

	printf("%d, %g\n", *int_arr, *double_arr);  // 20, 11.5
	printf("%d, %g\n", *int_arr + 1, *double_arr + 1);  // 21, 12.5
	printf("%d, %g\n", *(int_arr + 1), *(double_arr + 1));  // 20, 2.0



	for (int i = 1; i <= 5; i++)
	{
		for (int j = 1; j <= 5; j++)
		{
			printf("*");
		}
		printf("\n");
	}


}*/