#include <stdio.h>

int main(void)
{
	// 배열 Array
	/*int arr1[] = {5, 10, 15, 20, 25, 30, 35}; // 7개
	double arr2[] = { 5.0, 10.0, 15.0, 20.0 };  // 4개
	
	int arr1_len, arr2_len; // 배열의 갯수를 변수로

	arr1_len = sizeof(arr1) / sizeof(int);    // 7개
	arr2_len = sizeof(arr2) / sizeof(double); // 4개

	for (int i = 0; i < arr1_len; i++) {
		printf("%d\n", arr1[i]);
	}
	for (int i = 0; i < arr2_len; i++) {
		printf("%.1f\n", arr2[i]);
	}



	int arr1[] = { 1, 2, 3, 4, 5 };
	double arr2[] = { 1.0, 2.0, 3.0, 4.0 };

	int arr1_len, arr2_len;

	arr1_len = sizeof(arr1) / sizeof(int);
	arr2_len = sizeof(arr2) / sizeof(double);

	for (int i = 0; i < arr1_len; i++) {
		printf("%d, ", arr1[i]);
	}
	printf("\n");
	for (int i = 0; i < arr2_len; i++) {
		printf("%.1f, ", arr2[i]);
	}


	int arr1[] = { 1, 2, 3, 4, 5 };
	int arr2[] = { 1.0, 2.0, 3.0 };

	int arr1_len, arr2_len;
	int sum1 = 0;
	double sum2 = 0;

	arr1_len = sizeof(arr1) / sizeof(int);
	arr2_len = sizeof(arr2) / sizeof(double);
	printf("%d", arr2_len);

	for (int i = 0; i < arr1_len; i++) {
		sum1 += arr1[i];
	}

	int i = 0;
	while (i < arr1_len)
	{
		sum1 += arr1[i];
		i++;
	}

	int i = 0;
	do {
		sum1 += arr1[i];
		i++;
	} while (i < arr1_len);

	printf("arr1의 총합은 %d 입니다.\n", sum1);

	for (int i = 0; i < arr2_len; i++) {
		sum2 += arr2[i];
	}
	printf("arr2의 총합은 %f 입니다.\n", sum2);
}*/